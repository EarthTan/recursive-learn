export default function SearchPage() {
  return (
    <main className="search-page">
      <h1>Search</h1>
      <p>Keyword search across topics, nodes, and concepts can be added as a follow-up. Use each page’s own filter for now.</p>
    </main>
  );
}
